[project]
name = "biopathnet"
version = "0.1.0"
requires-python = ">=3.7,<3.9"
dependencies = [
	"torch==1.8.0",
]

[tool.uv.extra-build-dependencies]
	torch-scatter = ["torch"]
	torch-cluster = ["torch"]
